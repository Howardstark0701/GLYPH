# Copy this to .env.local for local dev, or set in Vercel dashboard for production
# The deployed Render backend URL — set this after the backend is live
PUBLIC_API_BASE_URL=https://glyph-api-u495.onrender.com
