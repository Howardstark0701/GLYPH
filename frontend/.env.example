# Copy this to .env.local for local dev, or set in Vercel dashboard for production
# The deployed Shuttle.rs backend URL — update after backend is deployed
PUBLIC_API_BASE_URL=https://glyph-<your-shuttle-subdomain>.shuttleapp.rs
